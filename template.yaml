AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Telegram Bot on AWS Lambda

Parameters:
  TelegramToken:
    Type: String
    Description: Telegram Bot Token
    NoEcho: true
  KandinskyApiKey:
    Type: String
    Description: Kandinsky API Key
    NoEcho: true
  KandinskyApiSecret:
    Type: String
    Description: Kandinsky API Secret
    NoEcho: true

Resources:
  TelegramBotFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: src.lambda_function.lambda_handler
      Runtime: python3.9
      Timeout: 60  # Увеличиваем таймаут до 60 секунд
      MemorySize: 512  # Увеличиваем память до 512 МБ
      Environment:
        Variables:
          TELEGRAM_TOKEN: !Ref TelegramToken
          KANDINSKY_API_KEY: !Ref KandinskyApiKey
          KANDINSKY_API_SECRET: !Ref KandinskyApiSecret  # Changed from KANDINSKY_SECRET_KEY
      Policies:
        - AWSLambdaBasicExecutionRole  # Allows Lambda to create logs in CloudWatch
        - Statement:
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource: '*'
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /webhook
            Method: post

Outputs:
  TelegramBotApi:
    Description: "API Gateway endpoint URL for Prod stage"
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/webhook"
  TelegramBotFunction:
    Description: "Lambda Function ARN"
    Value: !GetAtt TelegramBotFunction.Arn